class Spaces.ViewController

  constructor: (container, args...) ->
    @$container = $ container
    @initialize(args)

  initialize: ->
    # intentionally do nothing here.
    # override me in extending objects!